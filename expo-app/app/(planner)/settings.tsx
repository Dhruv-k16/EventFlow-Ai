import SharedSettings from '../../components/SharedSettings'; // ✅ Fixed: was ../components/

export default function Settings() {
  return <SharedSettings role="Planner" initialName="Alex Danvers" />;
}
